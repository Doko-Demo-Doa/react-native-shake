# Workflow label
name: Build and publish to NPM

on:
  push:
    branches:
      - main

jobs:
  publish:
    name: Publish to NPM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm i -g typescript
      - run: yarn install --immutable
      - run: yarn prepare
      - uses: JS-DevTools/npm-publish@v4 # https://github.com/JS-DevTools/npm-publish
        with:
          token: ${{ secrets.NPM_TOKEN }}
